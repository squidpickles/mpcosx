{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = MpcPlaylistListController; 
            LANGUAGE = ObjC; 
            OUTLETS = {windowController = MpcWindowController; }; 
            SUPERCLASS = NSArrayController; 
        }, 
        {
            ACTIONS = {
                browser = id; 
                changeSong = id; 
                clearPlaylist = id; 
                closePlaylistNamingWindow = id; 
                next = id; 
                openPlaylistNamingWindow = id; 
                performSavePlaylist = id; 
                playPause = id; 
                prev = id; 
                progress = id; 
                random = id; 
                removeFromPlaylist = id; 
                selectNowPlaying = id; 
                shufflePlaylist = id; 
                stop = id; 
                time = id; 
                updateDatabase = id; 
                updatePlaylist = id; 
                updatePlaylistList = id; 
                updateStatus = id; 
                volume = id; 
            }; 
            CLASS = MpcWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                browser = NSBrowser; 
                dbUpdateIcon = NSImageView; 
                mainWindow = NSWindow; 
                newPlaylistName = NSTextField; 
                nowPlaying = NSTextView; 
                playPause = NSButton; 
                playlist = NSTableView; 
                playlistListController = id; 
                playlistNamingWindow = NSWindow; 
                prefsWindow = NSWindow; 
                progress = NSLevelIndicator; 
                random = NSButton; 
                time = NSTextField; 
                volume = NSSlider; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = NSLevelIndicator; LANGUAGE = ObjC; SUPERCLASS = NSControl; }
    ); 
    IBVersion = 1; 
}